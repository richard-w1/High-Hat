document.addEventListener('DOMContentLoaded', function(){
  // Chart creation disabled - using CSS ring instead
  const el = document.getElementById('donut');
  if(!el) return;
  // JavaScript chart removed to show only CSS ring
});
